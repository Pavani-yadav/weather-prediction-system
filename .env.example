# Yathin Meteora environment variables
PORT=3000
MONGODB_URI=mongodb://localhost:27017/yathin_meteora
JWT_SECRET=yathin-meteora-secret-key-change-in-production
# If MONGODB_URI is unset, an in-memory store is used (no MongoDB install needed for demo)
